# App
PORT=4000
NODE_ENV=production
FRONTEND_ORIGIN=http://localhost:4173  # o file:// si abres el HTML directo (ver nota CORS)

# MySQL
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=pristineHomes_user
DB_PASSWORD=pristineHome.206
DB_NAME=pristineHomes 

# Stripe

#Nota CORS/CSRF: si sirves el HTML como archivo local, 
#configura un origen de desarrollo (p. ej. Vite/Live Server) para permitir 
#cookies y cabeceras. Recomiendo servir el front desde http://localhost:5173.