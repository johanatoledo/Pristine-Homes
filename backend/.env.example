# App
PORT=4000
NODE_ENV=development
FRONTEND_ORIGIN=http://localhost:5173  # o file:// si abres el HTML directo (ver nota CORS)

# MySQL
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=pristine_homes
DB_PASSWORD=superseguro
DB_NAME=pristine_homes

# Stripe
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# MercadoPago
MP_ACCESS_TOKEN=APP_USR-xxx
MP_WEBHOOK_SECRET=random_string_para_firmar  # opcional si firmas

#Nota CORS/CSRF: si sirves el HTML como archivo local, 
#configura un origen de desarrollo (p. ej. Vite/Live Server) para permitir 
#cookies y cabeceras. Recomiendo servir el front desde http://localhost:5173.